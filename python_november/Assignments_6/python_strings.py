# Scenario 2: String Manipulation
# a. Output of the message length.

message = "Hello, Python learners!"

print(len(message))

# b.
print(message.lower())

# c.
print(message[7:13])

# d.
# The len() function returns the length of the string:
example = "Hi, bro!"
print(len(example))

# The lower() method returns a string in lowercase:
example = "Hi, bro!"
print(example.lower())

# unlike a function where we put a variable 
# in the body of a function, we access the method through a dot.

# strings in Python are arrays, with the help of square brackets 
# you can use access to the elements of the string as we did in the example c.
