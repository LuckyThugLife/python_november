# Scenario 1: Numbers and Casting
# a. 

num1 = 15.6
num2 = 8

print(num1)
print(num2)

# b. Converting the num2 to a floating point number 
# and multiplying it by the num1. Output the result.

num2 = float(num2)
print(num2)

print(num1 * num2)

# c.
# Type conversion in Python is performed using constructor functions:
# int() - creates an integer from an integer literal, a floating—point literal 
# (by removing all decimal places);
# float() — creates a floating—point number from an integer literal, a floating
# -point literal str() - creates a string from a variety of data types.

# in example a, we output the values of variables using the print() function
# in example b, we convert num2 to a floating-point number using the float() function 
# and multiply it by num1 using the multiplication operator. We output the result.
